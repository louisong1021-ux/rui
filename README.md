# Arch Linux 全自动安装脚本（不使用 archinstall）

这是一个用于 **Arch Linux 安装 ISO 环境** 的一键自动化安装脚本，  
**不依赖 archinstall**，适合版本频繁变动或需要高度可控安装流程的场景。

---

## ⚠️ 重要警告

- **会完全清空并重建磁盘 `/dev/sda`**
- 仅建议在 **虚拟机或测试环境** 中使用
- 必须使用 **UEFI 模式启动 Arch ISO**
- 脚本为非交互式（除确认磁盘外）

---

## 🧩 安装内容

### 系统
- Arch Linux（官方仓库）
- GPT 分区 + UEFI 启动
- GRUB 引导

### 桌面环境
- **XFCE4**
- LightDM + GTK Greeter

### 预装软件
- NetworkManager（网络）
- sudo（管理员权限）
- open-vm-tools（VMware 支持）
- vim、git
- Noto 字体（含中文）

### 本地化
- 语言：**zh_CN.UTF-8**
- 键盘：US
- 时区：America/Los_Angeles

### 用户
- root 密码：`root`
- 普通用户：`rui`
- 用户密码：`123456`
- 用户默认具有 sudo 权限

> 本仓库为测试用途，未对密码进行保护。

---

## 🚀 使用方法（只需一行命令）

在 **Arch Linux 安装 ISO** 中，以 `root` 身份执行：

```bash
curl -fsSL https://raw.githubusercontent.com/louisong1021-ux/rui/main/install.sh | bash
